总结这个 Subsystem Test 环境的 Build, Run, and Regression Workflow。

重点关注：

1. 环境 setup、build/compile/elaboration、run/simulate/regression 命令。
2. testcase、scenario、firmware test、seed、boot mode、coverage、waveform 的选择方式。
3. log、wave、coverage、report、firmware log 等输出位置。
4. pass/fail 判断方式、failure triage、debug rerun 的第一步。

建议每个 item 包含：

- name
- description
- related_file_or_component
- signals_or_fields
- condition_or_behavior
- verification_points
- confidence
- evidence

