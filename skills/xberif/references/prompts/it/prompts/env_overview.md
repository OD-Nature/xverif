总结这个 IP Test 环境的 IP Verification Environment Overview。

重点关注：

1. testbench top、env class、agent、driver、monitor、sequencer、virtual sequencer。
2. scoreboard、reference model、coverage collector、register model、config object。
3. interface/virtual interface、DUT 实例化和连接。
4. build/connect/run/check/report phase 关键行为和 transaction 完整路径。

建议每个 item 包含：

- name
- description
- related_file_or_component
- signals_or_fields
- condition_or_behavior
- verification_points
- confidence
- evidence

