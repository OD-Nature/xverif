{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.axi.channel_stall.request.v1",
  "title": "axi.channel_stall request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "axi.channel_stall"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "channel": {
          "type": "string",
          "enum": [
            "aw",
            "w",
            "b",
            "ar",
            "r"
          ],
          "description": "AXI channel to inspect.",
          "x-description-zh": "AXI channel to inspect."
        },
        "line_limit": {
          "type": "integer",
          "minimum": 1,
          "description": "控制 response/xout 中 item、finding、event、transaction 或 row 的最大返回行数。",
          "x-description-zh": "控制 response/xout 中 item、finding、event、transaction 或 row 的最大返回行数。"
        },
        "name": {
          "type": "string",
          "description": "已保存配置、游标、列表或接口配置名称。",
          "x-description-zh": "已保存配置、游标、列表或接口配置名称。"
        },
        "rules": {
          "description": "AXI channel stall 阈值规则。",
          "x-description-zh": "本 action 的协议/检查规则对象；每个 rule 的默认和生效条件见嵌套字段说明。",
          "type": "object",
          "properties": {
            "max_wait_cycles": {
              "type": "integer",
              "minimum": 0,
              "default": 100,
              "description": "超过该连续 valid&&!ready sample 数才返回 long_stall finding。",
              "x-description-zh": "超过该连续 valid&&!ready sample 数才返回 long_stall finding。"
            }
          },
          "additionalProperties": false
        },
        "time_range": {
          "type": "object",
          "properties": {
            "begin": {
              "type": "string",
              "description": "args.time_range.begin 的 action-specific 参数值。",
              "x-description-zh": "args.time_range.begin 的 action-specific 参数值。"
            },
            "end": {
              "type": "string",
              "description": "args.time_range.end 的 action-specific 参数值。",
              "x-description-zh": "args.time_range.end 的 action-specific 参数值。"
            }
          },
          "additionalProperties": false,
          "description": "分析时间窗口；begin/end 为闭区间端点，可分别省略以使用可用波形边界。",
          "x-description-zh": "分析时间窗口；begin/end 为闭区间端点，可分别省略以使用可用波形边界。"
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "仅解释不带单位的时间数字；带单位时间字符串优先，auto 不改变带单位值。",
          "x-description-zh": "仅解释不带单位的时间数字；带单位时间字符串优先，auto 不改变带单位值。"
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Inspect AXI channel stall windows.",
  "x-purpose": "实验性 AXI stall 分析。",
  "x-how_it_works": "按 channel 检查 valid 高 ready 低的持续窗口。",
  "x-when_to_use": "定位背压和阻塞。",
  "x-arg_contract_notes": "required: name",
  "x-description-zh": "实验性 AXI stall 分析。",
  "x-agent": {
    "use_when": [
      "需要执行该 action 的 catalog 所述能力。"
    ],
    "do_not_use_when": [
      "任务不属于该 action 的公开合同。"
    ],
    "alternatives": [],
    "constraints": [
      "required: name"
    ]
  }
}
