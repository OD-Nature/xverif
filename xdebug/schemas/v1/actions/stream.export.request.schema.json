{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.stream.export.request.v1",
  "title": "stream.export request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "action": {
      "type": "string",
      "enum": [
        "stream.export"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "required": [
        "stream"
      ],
      "properties": {
        "channel": {
          "type": "string",
          "description": "args.channel 的 action-specific 参数值。",
          "x-description-zh": "args.channel 的 action-specific 参数值。"
        },
        "kind": {
          "type": "string",
          "enum": [
            "transfer",
            "packet",
            "packet_beats"
          ],
          "description": "导出或查询的结果类型。",
          "x-description-zh": "导出或查询的结果类型。"
        },
        "line_limit": {
          "type": "integer",
          "minimum": 1,
          "description": "控制 response/xout 中 item、finding、event、transaction 或 row 的最大返回行数。",
          "x-description-zh": "控制 response/xout 中 item、finding、event、transaction 或 row 的最大返回行数。"
        },
        "output": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "description": "args.output.path 的 action-specific 参数值。",
              "x-description-zh": "args.output.path 的 action-specific 参数值。"
            },
            "file_format": {
              "type": "string",
              "enum": [
                "tsv",
                "csv",
                "xout"
              ],
              "description": "stream.export file format.",
              "x-description-zh": "stream.export file format."
            },
            "verbose": {
              "type": "boolean",
              "description": "args.output.verbose 的 action-specific 参数值。",
              "x-description-zh": "args.output.verbose 的 action-specific 参数值。"
            }
          },
          "additionalProperties": false,
          "description": "导出目标与显示控制；不同 action 对 path、file_format 和 verbose 的支持以本 action schema 为准。",
          "x-description-zh": "导出目标与显示控制；不同 action 对 path、file_format 和 verbose 的支持以本 action schema 为准。"
        },
        "stream": {
          "type": "string",
          "description": "已保存 stream 配置名称。",
          "x-description-zh": "已保存 stream 配置名称。"
        },
        "time_range": {
          "type": "object",
          "properties": {
            "begin": {
              "type": "string",
              "description": "args.time_range.begin 的 action-specific 参数值。",
              "x-description-zh": "args.time_range.begin 的 action-specific 参数值。"
            },
            "end": {
              "type": "string",
              "description": "args.time_range.end 的 action-specific 参数值。",
              "x-description-zh": "args.time_range.end 的 action-specific 参数值。"
            }
          },
          "additionalProperties": false,
          "description": "分析时间窗口；begin/end 为闭区间端点，可分别省略以使用可用波形边界。",
          "x-description-zh": "分析时间窗口；begin/end 为闭区间端点，可分别省略以使用可用波形边界。"
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "仅解释不带单位的时间数字；带单位时间字符串优先，auto 不改变带单位值。",
          "x-description-zh": "仅解释不带单位的时间数字；带单位时间字符串优先，auto 不改变带单位值。"
        }
      },
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "required": [
              "output"
            ],
            "properties": {
              "output": {
                "required": [
                  "path"
                ],
                "properties": {
                  "path": {
                    "type": "string",
                    "minLength": 1
                  }
                }
              }
            }
          },
          "then": {
            "not": {
              "required": [
                "line_limit"
              ]
            }
          }
        }
      ]
    },
    "limits": {
      "type": "object"
    },
    "request_id": {
      "type": "string"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Export stream transfers or packet data.",
  "x-purpose": "导出 stream 查询结果。",
  "x-how_it_works": "运行 stream query 后按 kind/format 写出；kind 只能是 transfer、packet、packet_beats。",
  "x-when_to_use": "把流数据输出给外部脚本。",
  "x-arg_contract_notes": "required: stream; kind: transfer / packet / packet_beats; format: tsv / csv / xout; when kind=packet_beats: output",
  "x-description-zh": "导出 stream 查询结果。",
  "x-agent": {
    "use_when": [
      "需要执行该 action 的 catalog 所述能力。"
    ],
    "do_not_use_when": [
      "任务不属于该 action 的公开合同。"
    ],
    "alternatives": [],
    "constraints": [
      "required: stream; kind: transfer / packet / packet_beats; format: tsv / csv / xout; when kind=packet_beats: output"
    ]
  }
}
