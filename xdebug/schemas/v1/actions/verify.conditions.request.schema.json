{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.verify.conditions.request.v1",
  "title": "verify.conditions request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "verify.conditions"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "clock": {
          "type": "string",
          "description": "采样、统计或协议检查使用的 clock 信号路径。",
          "x-description-zh": "采样、统计或协议检查使用的 clock 信号路径。"
        },
        "conditions": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "expr"
            ],
            "properties": {
              "expr": {
                "type": "string",
                "description": "Expression using aliases from args.signals.",
                "x-description-zh": "Expression using aliases from args.signals."
              },
              "name": {
                "type": "string",
                "description": "args.conditions[].name 的 action-specific 参数值。",
                "x-description-zh": "args.conditions[].name 的 action-specific 参数值。"
              },
              "mode": {
                "type": "string",
                "enum": [
                  "always",
                  "eventually",
                  "never"
                ],
                "description": "args.conditions[].mode 的 action-specific 参数值。",
                "x-description-zh": "args.conditions[].mode 的 action-specific 参数值。"
              }
            },
            "additionalProperties": false,
            "description": "args.conditions[] 的组合参数对象。",
            "x-description-zh": "args.conditions[] 的组合参数对象。"
          },
          "description": "需要验证的条件列表。",
          "x-description-zh": "需要验证的条件列表。"
        },
        "edge": {
          "type": "string",
          "description": "clock sampling 使用的边沿：posedge、negedge 或 dual。",
          "x-description-zh": "clock sampling 使用的边沿：posedge、negedge 或 dual。"
        },
        "sample_point": {
          "type": "string",
          "enum": [
            "before",
            "after"
          ],
          "description": "posedge/dual 时的采样点：before 或 after；posedge 默认推荐 before。",
          "x-description-zh": "posedge/dual 时的采样点：before 或 after；posedge 默认推荐 before。"
        },
        "signals": {
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string",
                "description": "args.signals[] 的 action-specific 参数值。",
                "x-description-zh": "args.signals[] 的 action-specific 参数值。"
              },
              "description": "args.signals 的有序项目列表。",
              "x-description-zh": "args.signals 的有序项目列表。"
            },
            {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              },
              "description": "args.signals 的组合参数对象。",
              "x-description-zh": "args.signals 的组合参数对象。"
            }
          ],
          "description": "信号列表，或 alias 到信号路径的映射。",
          "x-description-zh": "信号列表，或 alias 到信号路径的映射。"
        },
        "time": {
          "type": "string",
          "description": "查询或验证的目标时间点。",
          "x-description-zh": "查询或验证的目标时间点。"
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "仅解释不带单位的时间数字；带单位时间字符串优先，auto 不改变带单位值。",
          "x-description-zh": "仅解释不带单位的时间数字；带单位时间字符串优先，auto 不改变带单位值。"
        },
        "value_format": {
          "type": "string",
          "enum": [
            "hex",
            "bin",
            "dec"
          ],
          "description": "仅控制响应中信号值的显示格式；不影响采样、时间或导出文件格式。",
          "x-description-zh": "仅控制响应中信号值的显示格式；不影响采样、时间或导出文件格式。"
        }
      },
      "additionalProperties": false,
      "required": [
        "conditions",
        "time",
        "clock",
        "signals"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Verify multiple conditions at one sampled time.",
  "x-purpose": "在单个时间验证条件集合。",
  "x-how_it_works": "解析 clock/time，按 args.signals 采样 alias，并对 conditions[].expr 求值。",
  "x-when_to_use": "检查某个时间点的多条件事实。",
  "x-arg_contract_notes": "required: conditions, time, clock, signals",
  "x-description-zh": "在单个时间验证条件集合。",
  "x-agent": {
    "use_when": [
      "需要执行该 action 的 catalog 所述能力。"
    ],
    "do_not_use_when": [
      "任务不属于该 action 的公开合同。"
    ],
    "alternatives": [],
    "constraints": [
      "required: conditions, time, clock, signals"
    ]
  }
}
