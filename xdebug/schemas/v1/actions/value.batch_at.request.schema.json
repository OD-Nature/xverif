{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.value.batch_at.request.v1",
  "title": "value.batch_at request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "value.batch_at"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "clock": {
          "type": "string",
          "description": "Action-specific clock input. Its type and accepted values are defined by this schema."
        },
        "edge": {
          "type": "string",
          "description": "Clock sampling edge. The schema default is authoritative; negedge often matches monitor semantics."
        },
        "format": {
          "type": "string",
          "enum": [
            "h",
            "hex",
            "b",
            "bin",
            "binary",
            "d",
            "dec",
            "decimal",
            "array_indexed",
            "json",
            "tsv",
            "csv",
            "u64bin"
          ],
          "description": "Action-specific format input. Its type and accepted values are defined by this schema."
        },
        "sample_point": {
          "type": "string",
          "enum": [
            "before",
            "after"
          ],
          "description": "Before/after observation point for posedge or dual sampling; it does not change the raw waveform range."
        },
        "signals": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
          },
          "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
        },
        "slice_hint": {
          "type": "object",
          "description": "Structured slice_hint configuration. Only the declared properties are accepted."
        },
        "time": {
          "type": "string",
          "description": "Target sample time. Prefer a canonical string with a unit; a bare number is interpreted only with time_unit."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        },
        "value_format": {
          "type": "string",
          "enum": [
            "hex",
            "bin",
            "dec"
          ],
          "description": "Action-specific value_format input. Its type and accepted values are defined by this schema."
        }
      },
      "additionalProperties": false,
      "required": [
        "signals",
        "time"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Read multiple signals at one exact waveform time, optionally using clock-sampled context.",
  "x-purpose": "批量读取多个信号值。",
  "x-how_it_works": "无 clock 时直接批量点读 time；有 clock 时返回同一 clock/time 的采样上下文。",
  "x-when_to_use": "减少多信号同一时刻检查的开销。",
  "x-arg_contract_notes": "required: signals, time; optional: clock, edge, sample_point",
  "x-description-zh": "批量读取多个信号值。",
  "x-agent": {
    "use_when": [
      "需要多个最终叶子信号在同一精确时间的 raw 值，或显式 clock 采样上下文。"
    ],
    "do_not_use_when": [
      "需要原始值变化时间线。",
      "需要跨时间窗口验证条件。"
    ],
    "alternatives": [
      {
        "action": "signal.changes",
        "when": "需要每次原始值变化。"
      },
      {
        "action": "window.verify",
        "when": "需要跨 clock 窗口证明条件。"
      }
    ],
    "constraints": [
      "required: signals, time; optional: clock, edge, sample_point"
    ]
  }
}
