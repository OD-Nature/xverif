{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.axi.latency_outlier.request.v1",
  "title": "axi.latency_outlier request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "axi.latency_outlier"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "direction": {
          "type": "string",
          "enum": [
            "write",
            "read",
            "all"
          ],
          "description": "args.direction 的 action-specific 参数值。",
          "x-description-zh": "args.direction 的 action-specific 参数值。"
        },
        "line_limit": {
          "type": "integer",
          "minimum": 1,
          "description": "控制 response/xout 中 item、finding、event、transaction 或 row 的最大返回行数。",
          "x-description-zh": "控制 response/xout 中 item、finding、event、transaction 或 row 的最大返回行数。"
        },
        "method": {
          "type": "string",
          "enum": [
            "top_n",
            "threshold"
          ],
          "default": "top_n",
          "description": "top_n 返回完整候选集中的最慢事务排名；threshold 返回超过阈值的事务。",
          "x-description-zh": "top_n 返回完整候选集中的最慢事务排名；threshold 返回超过阈值的事务。"
        },
        "name": {
          "type": "string",
          "description": "已保存配置、游标、列表或接口配置名称。",
          "x-description-zh": "已保存配置、游标、列表或接口配置名称。"
        },
        "output": {
          "type": "object",
          "properties": {
            "include_data": {
              "type": "boolean",
              "default": false,
              "description": "args.output.include_data 的 action-specific 参数值。",
              "x-description-zh": "args.output.include_data 的 action-specific 参数值。"
            }
          },
          "additionalProperties": false,
          "description": "AXI payload display controls; beat data is omitted unless include_data is true.",
          "x-description-zh": "AXI payload display controls; beat data is omitted unless include_data is true."
        },
        "threshold": {
          "type": "string",
          "description": "method=threshold 时必填的延迟阈值，例如 100ns。",
          "x-description-zh": "method=threshold 时必填的延迟阈值，例如 100ns。"
        },
        "time_range": {
          "type": "object",
          "properties": {
            "begin": {
              "type": "string",
              "description": "args.time_range.begin 的 action-specific 参数值。",
              "x-description-zh": "args.time_range.begin 的 action-specific 参数值。"
            },
            "end": {
              "type": "string",
              "description": "args.time_range.end 的 action-specific 参数值。",
              "x-description-zh": "args.time_range.end 的 action-specific 参数值。"
            }
          },
          "additionalProperties": false,
          "description": "分析时间窗口；begin/end 为闭区间端点，可分别省略以使用可用波形边界。",
          "x-description-zh": "分析时间窗口；begin/end 为闭区间端点，可分别省略以使用可用波形边界。"
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "仅解释不带单位的时间数字；带单位时间字符串优先，auto 不改变带单位值。",
          "x-description-zh": "仅解释不带单位的时间数字；带单位时间字符串优先，auto 不改变带单位值。"
        },
        "top_n": {
          "type": "integer",
          "minimum": 1,
          "default": 10,
          "description": "method=top_n 时，从完整候选集选择的最慢事务数量。",
          "x-description-zh": "method=top_n 时，从完整候选集选择的最慢事务数量。"
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Find AXI transactions with abnormal latency.",
  "x-purpose": "实验性 AXI latency 异常。",
  "x-how_it_works": "从配对结果中找超过阈值或分布异常的事务。",
  "x-when_to_use": "定位慢事务。",
  "x-arg_contract_notes": "required: name",
  "x-description-zh": "实验性 AXI latency 异常。",
  "x-agent": {
    "use_when": [
      "需要执行该 action 的 catalog 所述能力。"
    ],
    "do_not_use_when": [
      "任务不属于该 action 的公开合同。"
    ],
    "alternatives": [],
    "constraints": [
      "required: name"
    ]
  }
}
