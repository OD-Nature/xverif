{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.apb.config.load.request.v1",
  "title": "apb.config.load request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "apb.config.load"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "config": {
          "type": "object",
          "description": "内联配置对象。",
          "properties": {
            "clock": {
              "type": "string",
              "minLength": 1,
              "description": "args.config.clock 的 action-specific 参数值。",
              "x-description-zh": "args.config.clock 的 action-specific 参数值。"
            },
            "rst_n": {
              "type": "string",
              "minLength": 1,
              "description": "args.config.rst_n 的 action-specific 参数值。",
              "x-description-zh": "args.config.rst_n 的 action-specific 参数值。"
            },
            "edge": {
              "type": "string",
              "enum": [
                "posedge",
                "negedge",
                "dual"
              ],
              "description": "args.config.edge 的 action-specific 参数值。",
              "x-description-zh": "args.config.edge 的 action-specific 参数值。"
            },
            "sample_point": {
              "type": "string",
              "enum": [
                "before",
                "after"
              ],
              "description": "args.config.sample_point 的 action-specific 参数值。",
              "x-description-zh": "args.config.sample_point 的 action-specific 参数值。"
            },
            "paddr": {
              "type": "string",
              "minLength": 1,
              "description": "args.config.paddr 的 action-specific 参数值。",
              "x-description-zh": "args.config.paddr 的 action-specific 参数值。"
            },
            "psel": {
              "type": "string",
              "minLength": 1,
              "description": "args.config.psel 的 action-specific 参数值。",
              "x-description-zh": "args.config.psel 的 action-specific 参数值。"
            },
            "penable": {
              "type": "string",
              "minLength": 1,
              "description": "args.config.penable 的 action-specific 参数值。",
              "x-description-zh": "args.config.penable 的 action-specific 参数值。"
            },
            "pready": {
              "type": "string",
              "minLength": 1,
              "description": "args.config.pready 的 action-specific 参数值。",
              "x-description-zh": "args.config.pready 的 action-specific 参数值。"
            },
            "pslverr": {
              "type": "string",
              "minLength": 1,
              "description": "args.config.pslverr 的 action-specific 参数值。",
              "x-description-zh": "args.config.pslverr 的 action-specific 参数值。"
            },
            "pwrite": {
              "type": "string",
              "minLength": 1,
              "description": "args.config.pwrite 的 action-specific 参数值。",
              "x-description-zh": "args.config.pwrite 的 action-specific 参数值。"
            },
            "pwdata": {
              "type": "string",
              "minLength": 1,
              "description": "args.config.pwdata 的 action-specific 参数值。",
              "x-description-zh": "args.config.pwdata 的 action-specific 参数值。"
            },
            "prdata": {
              "type": "string",
              "minLength": 1,
              "description": "args.config.prdata 的 action-specific 参数值。",
              "x-description-zh": "args.config.prdata 的 action-specific 参数值。"
            }
          },
          "required": [
            "clock",
            "rst_n",
            "paddr",
            "psel",
            "penable",
            "pready",
            "pslverr",
            "pwrite",
            "pwdata",
            "prdata"
          ],
          "additionalProperties": false,
          "x-description-zh": "内联配置对象。"
        },
        "config_path": {
          "type": "string",
          "description": "输入配置文件路径。",
          "x-description-zh": "输入配置文件路径。"
        },
        "name": {
          "type": "string",
          "description": "已保存配置、游标、列表或接口配置名称。",
          "x-description-zh": "已保存配置、游标、列表或接口配置名称。"
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "仅解释不带单位的时间数字；带单位时间字符串优先，auto 不改变带单位值。",
          "x-description-zh": "仅解释不带单位的时间数字；带单位时间字符串优先，auto 不改变带单位值。"
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "anyOf": [
        {
          "required": [
            "config"
          ]
        },
        {
          "required": [
            "config_path"
          ]
        }
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Load and validate an APB interface configuration.",
  "x-purpose": "加载 APB 配置。",
  "x-how_it_works": "保存 APB interface 信号映射。",
  "x-when_to_use": "定义后续 APB 查询对象。",
  "x-arg_contract_notes": "required: name; also one of: config / config_path",
  "x-description-zh": "加载 APB 配置。",
  "x-agent": {
    "use_when": [
      "需要执行该 action 的 catalog 所述能力。"
    ],
    "do_not_use_when": [
      "任务不属于该 action 的公开合同。"
    ],
    "alternatives": [],
    "constraints": [
      "required: name; also one of: config / config_path"
    ]
  }
}
